import React from "react";

const VideoPlayerControls = () => {
  return (
    <div>
      CONTROSL
      {/* <button onClick={togglePlayPaused} className="relative z-50">
        Payse/play
      </button>
      <p>{videoProgress}</p> */}
    </div>
  );
};

export default VideoPlayerControls;
